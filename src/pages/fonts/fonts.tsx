import localFont from "next/font/local";

export const Helvetical = localFont({
    src: "./HelveticaNeue.otf",
    variable: "--font-geist-sans",
    weight: "100 900", // Specify the range as a string
  });
  